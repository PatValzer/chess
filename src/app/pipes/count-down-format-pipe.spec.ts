import { CountDownFormatPipePipe } from './count-down-format-pipe';

describe('CountDownFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountDownFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
